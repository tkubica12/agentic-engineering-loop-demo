# Normalise line endings in the repository so the same pattern matches on every
# platform. The working tree keeps whatever the platform prefers.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gz binary

# Compiled agentic workflows are generated by `gh aw compile`.
.github/workflows/*.lock.yml linguist-generated=true
